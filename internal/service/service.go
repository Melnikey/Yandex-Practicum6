package service
import (
	"strings"
	"pkg/morse"
)

func Convert(x string) (y string, err error) {
 prefix1 := "."
 prefix2 := "-"
 if strings.HasPrefix(x, prefix1) || strings.HasPrefix(x, prefix2) {
	To
 }
	return y, nil
} 